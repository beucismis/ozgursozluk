<?xml version="1.0" encoding="UTF-8"?>
<rss version="2.0">
  <channel>
    <title>{{ topic.title }}</title>
    <link>{{ url_for("topic", path=topic.path, _external=True) }}</link>
    <language>tr</language>
    {% for entry in topic.entrys %}
    <item>
      <title>{{ topic.title }}</title>
      <link>{{ url_for("entry", id=entry.id, _external=True) }}</link>
      <description><![CDATA[ {{ entry.html | safe }} ]]></description>
      <pubDate>{{ entry.created }}</pubDate>
    </item>
    {% endfor %}
  </channel>
</rss>
